export class User {
    constructor({ email, password, nombre, apellido, rol }) {
      this.email = email
      this.password = password
      this.nombre = nombre
      this.apellido = apellido
      this.rol = rol
    }
  }